# Upload-file

Component that downloads a file from a source URL, temporarily stores it locally, and uploads it to a target destination (e.g., S3).

## Building

Requires rust with target wasm32-wasip2 installed and the `wash` tool for fetching the dependencies and building the component.

run:

```sh
just build
```
