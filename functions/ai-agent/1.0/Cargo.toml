[package]
name = "ai_agent"
version = "1.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.51"
wstd = "0.6.5"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
