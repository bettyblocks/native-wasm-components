package betty-blocks:ai-agent@2.0.0;

interface ai-agent {
    use betty-blocks-types:types/types@2.3.0.{betty-ai-provider};

    ai-agent: func(
        provider: betty-ai-provider,
        system-prompt: string,
        prompt: string,
        max-tokens: option<u32>,
    ) -> result<string, string>;
}

world main {
    import wasi:config/store@0.2.0-rc.1;

    export ai-agent;
}
